export namespace Log{
    export interface ResLog{
        id: number;
        organize: number;
        user: string;
        category: string;
        ip: string;
        city: string;
        date: string;
        detail: string;
    }
}