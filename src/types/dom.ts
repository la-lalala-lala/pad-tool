export namespace Dom {
    export interface TemplateTransform {
        //unsetTransform: string; // 默认值滑动
        setTransform: string; // 设置后的滑动值
        //unsetWidth: number; // 默认宽度值
        setWidth: string; // 设置后的宽度值
    }
}